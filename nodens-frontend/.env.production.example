# Production contact form — set in Cloudflare Pages, NOT in this file on Git.
# Cloudflare: Settings → Environment variables → Production → VITE_WEB3FORMS_KEY
# After saving, Deployments → ⋯ → Retry deployment (Vite bakes env at build time).
VITE_WEB3FORMS_KEY=your_web3forms_access_key_here
